/**
 * Exporting Hook registry and HookService so that plugins can use them without rewriting the code.
 */
export { HookRegistry } from '@/features/hooks/HookRegistry.js'
export { HookService } from '@/features/hooks/HookService.js'
