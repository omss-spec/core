import type OMSSServer from '@/core/server.js'

export class SourceService {
    readonly #server: OMSSServer

    constructor(server: OMSSServer) {
        this.#server = server
    }

    /// resolver and provider handler
}
