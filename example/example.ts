import { OMSSServer } from '../src/index.js'

const server = new OMSSServer({ name: 'example' })

// create an example of all possibilities with the server
// plugins, hooks, providers, resolvers, sourcegetting, providerhooks etc
